/* Tutorial 4
    Example 6 JavaScript code
*/

hour = prompt("Enter the hours");
minutes = prompt("Enter the minutes");
seconds = prompt("Enter the seconds");
let Time = concat(hour,minutes,seconds);

if(hour >= 0 && minutes >= 0 && seconds >= 0){
    
}
